# 개요
- 배열에서 많이 쓰이는 내부 함수

# 종류
## push()
- 어규먼트를 배열 마지막에 직접 넣음
- 반환값 : 추가된 후의 배열 길이 반환

## pop()
- 마지막 요소를 뽑아냄
- 반환값 : 뽑아낸 요소

## shift()
- 처음 요소를 뽑아냄
- 반환값 : 뽑아낸 요소

## unshift()
- 어규먼트를 배열 처음에 직접 넣음
- 반환값 : 추가된 후의 배열 길이 반환

## splice(a, b)
- a 부터 b 인덱스까지의 요소를 잘라서 뱉음
- 반환값으로 뱉어진 요소들 반환